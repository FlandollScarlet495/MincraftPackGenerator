# Minecraft Pack Generator 必要ライブラリ
# 基本的なPython標準ライブラリのみを使用しているため、追加のライブラリは不要です

# 開発用ライブラリ（exeファイル作成時のみ必要）
pyinstaller>=5.0.0

# オプション：ログの色付け（より見やすいログ出力のため）
colorama>=0.4.0

# オプション：プログレスバー（進捗表示のため）
tqdm>=4.64.0

# オプション：設定ファイルの検証（より厳密な検証のため）
jsonschema>=4.0.0

# オプション：画像処理（テクスチャの自動リサイズなど）
Pillow>=9.0.0 